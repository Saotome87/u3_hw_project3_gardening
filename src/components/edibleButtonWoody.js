const EdibleButtonWoody = (props) => {
  return (
    <button className="edible-button-woody" id="btn" onClick={props.toggleInfo}>
      Woody
    </button>
  );
};

export default EdibleButtonWoody;
